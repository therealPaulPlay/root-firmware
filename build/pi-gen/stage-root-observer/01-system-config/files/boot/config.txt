# ROOT boot configuration

# A/B slot selection (cmdline_a.txt or cmdline_b.txt)
cmdline=cmdline_a.txt

# Enable 64-bit mode
arm_64bit=1

# Camera (set by create-config-variants.sh in build step)
CAMERA_CONFIG_PLACEHOLDER

# Enable I2C
dtparam=i2c_arm=on

# Disable splash screen for faster boot
disable_splash=1

# Serial console
enable_uart=1

# Audio
dtparam=audio=on